import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    flexGrow: 1
  },
  paper: {
    background: '#cbc548',
    margin: 'auto',
    padding: 20,
    maxWidth: 500
  },
  avatar: {
    width: 64,
    height: 64
  }
})

export default useStyles
